# Word Guessing Game – Python

Simple console game (Hangman-like). Uses a large wordlist.

## Setup
1. Download `words_alpha.txt` (from dwyl) into the project folder.
2. (Optional) Create virtual env:
   - `python -m venv .venv`
   - `source .venv/bin/activate` (or `.\.venv\Scripts\Activate.ps1` on Windows)
3. Run:
```bash
python game.py
